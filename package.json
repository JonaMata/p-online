{
  "name": "p-server",
  "version": "0.0.1",
  "description": "A server for p-online",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Juan To 3",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^1.5.1"
  }
}
